# EdgeHub 1.0.0 release ledger

This file is the source-controlled ledger for the first stable release. It
records what was actually verified, what remains untested, and which facts need
owner confirmation. A checked item is never inferred from a skipped test.

## Candidate lineage

- Release branch: `release/1.0.0`
- Per-commit workflow coverage: `ci.yml`, `docs.yml`, `distro.yml`, and
  `supply-chain.yml` include the active release branch and pull requests for
  their relevant paths. Pull-request package jobs keep build/install/smoke
  coverage while OIDC attestations remain non-pull-request only.
- Frozen base commit: `64d4ca89fb9afa84ce623bc3f4a60a808d9998bf`
- Base identity: `v1.0.0-beta.1-103-g64d4ca8`
- Feature scope: frozen. Only release blockers, test/evidence work, packaging,
  documentation truth, and fixes required by those gates may enter 1.0.0.
- Owner-machine package installed from the clean base:
  `1.0.0.beta.1.r103.g64d4ca8-1`
- Installed Hub and Manager reported the exact base identity.
- `pacman -Qkk` reported zero altered package files immediately after install.
- The live configuration was backed up before replacement and remained
  byte-identical through the package transaction.

The installed base is a safe dogfood checkpoint, not the release candidate.
All release claims below must be repeated against the later clean, committed,
signed RC.

## Ordered release work

| Phase | Status | Exit condition |
|---|---|---|
| 1. Freeze feature scope | Complete | Release branch exists and scope above is frozen. |
| 2. Close known engineering blockers | In progress | Version identity, migration safety, third-party notices, network warning, orientation startup disposition, immutable evidence, install and autostart behavior all pass focused checks. |
| 3. Qualify the performance contract | Pending | Fresh non-instrumented Release candidate passes startup, five-minute idle, five-minute active, and the owner-approved literal 30-minute 14-widget observation. |
| 4. Publish the support contract | Pending | Supported distributions, desktops, sessions, architectures, package formats, display modes, and explicit best-effort exclusions are documented consistently. |
| 5. Cut a signed immutable RC | Pending | Final `RELEASE_NOTES.md` names exactly `v1.0.0`, lists the exact publication asset ledger, and is committed with the clean SHA; signed annotated tag, source archive, binaries, package artifacts, SBOM, checksums, signatures, and manifests agree. |
| 6. Certify the exact RC on hardware | Pending | Physical panel, touch, orientation, Manager integration, notification, MPRIS, reconnect, suspend, and display-targeting evidence is commit-keyed and hashed. |
| 7. Certify delivery lifecycle | Pending | Clean install, upgrade, rollback, uninstall, user-data preservation, autostart behavior, and every supported portable update path pass against exact artifacts. |
| 8. Complete RC field and strict gates | Pending | Required field cycle, automated release gate, stability duration, security checks, accessibility review, and accepted-risk ledger are complete. |
| 9. Publish 1.0.0 exact bytes | Pending | Final tag points at the certified SHA; the signed stable-publication receipt passes; public assets equal the signed local bytes; public install and rollback are reverified. |

## Current blocker ledger

| ID | Finding | Classification | Status |
|---|---|---|---|
| REL-001 | DEB and RPM builds could publish historical `0.1.0` metadata while binaries reported another version. | Release blocker | Fix in review |
| REL-002 | A clean local `makepkg` build could acquire a false `-dirty` identity when `makepkg` rewrote the tracked recipe. | Release blocker | Fix in review |
| REL-003 | A newer valid configuration schema could be loaded and later rewritten by an older build, dropping unknown data. | Data-loss blocker | Fix in review |
| REL-004 | Pre-migration configuration bytes were not automatically backed up. | Rollback blocker | Fix in review |
| REL-005 | Chakra Petch and Phosphor asset notices were incomplete in installed packages. | Legal blocker | Fix in review |
| REL-006 | Qt HTTP/2 cleanup emitted a recurrent `QSslSocket` read warning during the live network-widget workload. | Runtime-quality blocker | HTTPS is forced to HTTP/1.1 and a focused transport-policy test exists; clean-SHA live TLS rerun pending |
| REL-007 | Push-only panel firmware rejects startup `GET_REPORT`; the retry outcome was logged as a warning even when a valid remembered rotation safely covered startup. | Diagnostic-quality defect | Focused fix passes in development; clean-SHA rerun pending |
| REL-008 | Evidence can be hashed while a producer still has a file open, allowing a manifest to become stale immediately. | Audit-integrity blocker | Finalizer now requires closed producers, semantic v2 records, exact hashes, and a signed manifest; focused execution contract and clean-SHA gate pending |
| REL-009 | Current legal text describes Qt as LGPL-only, but Qt Virtual Keyboard is GPLv3-only for open-source use and is linked by the Hub. | Owner/legal blocker | Licence disposition required before RC |
| REL-010 | A published AppImage-to-AppImage zsync update lacks exact-candidate retained evidence. Automatic `latest` discovery is stable-only; prereleases require a documented versioned zsync URL. | Delivery blocker if AppImage is supported | Not tested |
| REL-011 | The historical 48-hour idle soak will not run for 1.0.0. | Owner-accepted risk, not a blocker | Explicitly waived; the literal 30-minute 14-widget instrumented observation is the required substitute and must pass on the exact RC |
| REL-012 | AppImage autostart or Manager `startHub()` could persist an ephemeral `/tmp/.mount_*` Hub path. | Delivery lifecycle blocker | Persistent `$APPIMAGE --hub` launch path and focused lifetime tests implemented; clean-SHA package smoke pending |
| REL-013 | An ignored source header could affect a local build without appearing in ordinary dirty-tree output. | Audit-integrity blocker | Release preflight now enumerates source-like files against the Git index; ignored-source and clean-clone contract added |
| REL-014 | `RELEASE_NOTES.md` still describes `v1.0.0-beta.2`, so it cannot truthfully serve as the stable `v1.0.0` publication record. | Editorial and release-integrity blocker | Required pre-RC owner/editorial step; the release contract rejects a stale version, stage, section set, or exact asset ledger before running the strict gate |
| REL-015 | Stable publication could proceed after the automated strict suite without typed signed receipts for physical touch, desktop notification, MPRIS, native two-version lifecycle, and published zsync gates. | Release-integrity blocker | Five typed prepublication receipts, explicit candidate staging, a separately signed public zsync receipt, metadata-only promotion, and focused negative controls are implemented; clean-SHA contract run pending |
| REL-016 | The published release history contains no AppImage, so no truthful prior-version AppImage exists for the first zsync update proof. | Delivery blocker | Publish an AppImage-bearing `v1.0.0-rc.1` candidate first; CI artifacts, packages, tarballs, and retrofitted beta assets do not satisfy the gate |

REL-011 is a deliberate owner waiver, not missing engineering work. The
30-minute substitute must retain the full sample trace, exact 14-widget live
state, startup result, CPU/RSS budgets, GPU-memory observations, and CPU, RSS,
file-descriptor, and thread slopes. Passing it does not claim 48-hour endurance.

## Evidence rules

- Every formal result starts from a clean committed SHA.
- Release preflight separately rejects source-like files outside the Git index,
  including ignored files and CMake-listed headers.
- Development tests on an uncommitted fix are feedback, not release evidence.
- Evidence lives under `artifacts/<full-40-character-commit>/<run-id>/`, never
  only under `/tmp`.
- The final manifest excludes itself and its detached signature, is generated
  only after producers close their files, is signed by the pinned release key,
  and is verified immediately.
- `PREFLIGHT.tsv`, `SUMMARY.tsv`, and `RUN.json` are semantic release records.
  The run record binds exact row counts and hashes; the release receives a
  mode-`0600` receipt and signs `RELEASE_GATE_EVIDENCE.json` into its payload.
- Stable candidate staging additionally requires a signed, commit-keyed
  `RELEASE_CERTIFICATION.json` that references five separately signed typed
  gate directories. Its exact receipt hashes are published through
  `RELEASE_CERTIFICATION_EVIDENCE.json`.
- Stable promotion is a separate metadata-only command. It requires the
  prepublication aggregate plus a separately signed post-publication AppImage
  zsync receipt. That receipt remains in the owner archive because it cannot be
  self-included in the staged asset set whose public URL it proves.
- `not tested`, `tested and failed`, and `tested and passed` remain distinct.
- No metric is called a gate unless a script or workflow enforces it.
- Public artifacts must be byte-for-byte equal to the signed local release set.
  Verification includes a fresh download after the release becomes public; a
  mismatch attempts to return it to draft and fails the release command.

## Owner inputs

Engineering continues without these until the named gate:

1. Confirm the Qt Virtual Keyboard licence disposition before RC metadata is
   finalized.
2. The owner reports that the release licence file is ready. Supply only its
   absolute protected path through `XENEON_TEST_LICENSE_KEY_FILE` when the
   strict runner requests it; never expose the key value or private issuer seed
   to a command line, process environment, artifact, or log.
3. Confirm physical actions when the exact RC reaches reconnect, power, suspend,
   and touch certification.
4. Review and sign the typed touch, notification, MPRIS, DEB, and RPM receipts,
   then sign their exact-commit prepublication aggregate. After staging, run and
   separately sign the public zsync receipt. Engineering must not create a PASS
   attestation in the owner's name.
5. Approve the public GitHub release only after the complete RC report is
   available.
